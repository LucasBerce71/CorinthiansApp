import React, { memo } from 'react';
import Header from '../../components/Header';

const Home: React.FC = () => {
    return (
        <>
            <Header />
        </>
    );
};

export default memo(Home);